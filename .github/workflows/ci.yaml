name: Build Workflow CI

on:
  push:
    branches: [main]
    paths:
      - "src/**"

permissions:
  contents: read
  packages: write

jobs:
  # --------------------------------------------------
  # Job1: Call reusable workflow (matrix per service)
  # --------------------------------------------------
  build-and-push:
    strategy:
      fail-fast: false
      matrix:
        service: [order]
        # service: ${{ fromJson(needs.detect-changes.outputs.services) }}

    # Reusable workflows are called at JOB level
    uses: ./.github/workflows/ci-workflow.yaml

    with:
      service: ${{ matrix.service }}
